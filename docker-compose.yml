version: "3.9"

services:
  postgres:
    image: postgres
    container_name: azheimer-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: azheimer_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12345
    ports:
      - "5432:5432"

volumes:
  pgdata:
